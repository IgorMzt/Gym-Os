(()=>{const box=document.querySelector('.student-load-chart-v591'),cv=document.getElementById('load-chart-v591');if(!box||!cv)return;const vals=JSON.parse(box.dataset.values||'[]').map(v=>{const m=String(v||'').replace(',','.').match(/[\d.]+/);return m?parseFloat(m[0]):null}).filter(v=>Number.isFinite(v));if(vals.length<2){cv.hidden=true;return}const dpr=devicePixelRatio||1,w=cv.clientWidth||300,h=180;cv.width=w*dpr;cv.height=h*dpr;const c=cv.getContext('2d');c.scale(dpr,dpr);const pad=20,min=Math.min(...vals),max=Math.max(...vals),span=max-min||1;c.lineWidth=3;c.beginPath();vals.forEach((v,i)=>{const x=pad+i*(w-pad*2)/(vals.length-1),y=h-pad-(v-min)*(h-pad*2)/span;i?c.lineTo(x,y):c.moveTo(x,y)});c.stroke();vals.forEach((v,i)=>{const x=pad+i*(w-pad*2)/(vals.length-1),y=h-pad-(v-min)*(h-pad*2)/span;c.beginPath();c.arc(x,y,4,0,Math.PI*2);c.fill()})})();